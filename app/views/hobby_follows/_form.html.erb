<%= form_with(model: hobby_follow) do |form| %>
  <%= form.hidden_field :hobby_id %>
  
  <% if hobby_follow.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(hobby_follow.errors.count, "error") %> prohibited this hobby_follow from being saved:</h2>

      <ul>
        <% hobby_follow.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
